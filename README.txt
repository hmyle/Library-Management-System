**IMPORTANT**
- The ISYS3414_DB_Anything_MySQL.sql file only works when you run the script in MySQL workbench, the file cannot be run on Oracle Apex as the file use MySQL code syntax
- The ISYS3414_DB_Anything_Oracle.sql file only works when you run the script in Oracle Apex, the file cannot be run on MySQL workbench as the file uses Oracle SQL code syntax
- The ISYS3414_Query_Anything_MySQL.sql file only works when you run the script in MySQL workbench, the file cannot be run on Oracle Apex as the file use MySQL code syntax
- The ISYS3414_Query_Anything_Oracle.sql file only works when you run the script in Oracle Apex, the file cannot be run on MySQL workbench as the file uses Oracle SQL code syntax